# Sound Swipe — Stage H Sound Lab

This build keeps the Stage G functionality and adds the Sound Lab visual layer: sound-pad lesson tiles, generated waveform motifs, audio-module answer cards, subtle grid textures, and playback pulse states.

Drop MP3 files into `assets/sounds/`, then open `index.html` or run `python -m http.server 8000` from this folder.
